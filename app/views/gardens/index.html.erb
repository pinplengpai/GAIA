<div class="home-banner">
  <div id="cta-wrapper">
    <span id="banner-typed">Make your event more</span>
    <div>
      <span id="banner-typed-text"><span class="typed-cursor"></span></span>
    </div>
  </div>
</div>

<!-- <div class="container">
  <div class="cards">
    <#% @gardens.each do |garden| %>
      <#%= link_to garden do  %>
        <div class="card m-3">
          <#%= cl_image_tag garden.picture %>
          <div class="card-text">
            <h2><#%= garden.name %></h2>
            <p><#%= garden.address %></p>
            <p class="price">$<#%= garden.price %>/day</p>
          </div>
        </div>
      <#% end %>
    <#% end %>
  </div>
</div> -->
<div class="container">
  <div class="cards">
    <% @gardens.each do |garden| %>
     <%= link_to garden do  %>
    <div class="card-trip m-3">
      <%= cl_image_tag garden.picture %>
      <div class="card-trip-infos">
        <div>
          <h2><%= garden.name %></h2>
          <p><%= garden.address %></p>
        </div>
        <h2 class="card-trip-pricing">$<%= garden.price %></h2>
      </div>
    </div>
    <% end %>
    <% end %>
  </div>
</div>
