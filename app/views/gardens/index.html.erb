<div class="home-banner">
  <h1>GAIA</h1>
</div>

<div class="container">
  <div class="cards">
    <% @gardens.each do |garden| %>
      <%= link_to garden do  %>
        <div class="card m-3">
          <%= cl_image_tag garden.picture %>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
